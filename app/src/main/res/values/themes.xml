<resources>

    <!-- Main theme -->
    <style name="Theme.Swipe_assignment" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash theme -->
    <style name="Theme.Swipe_assignment.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Theme.Swipe_assignment</item>
    </style>

</resources>
